RewriteEngine On

# Biarkan request ke file/folder yang memang ada (css, js, gambar) diproses langsung
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Selain itu, semua request diarahkan ke index.php (front controller)
RewriteRule ^ index.php [L]
